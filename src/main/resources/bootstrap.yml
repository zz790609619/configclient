spring:
  cloud:
    config:
      label: master
      discovery:
        enabled: true
        service-id: configsever
      fail-fast: true
      profile: master
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka/
